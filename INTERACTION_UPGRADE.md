# RoboCurate 界面与 10 分制升级

日常操作集中在 **任务、质检、审核** 三个主入口。关节曲线和原始通道移入“更多”，评分分项统一由“评分依据”打开。任务为默认入口。

视频下方只保留一条当前分析时间轴：任务模型曲线与质检事件随所选入口切换。模型侧栏保留当前阶段、输入有效性、最终结果待核验提示和待复核片段；完整概率、整段覆盖及方法说明放入“详细证据与方法”，模型选择放入“分析设置”。阶段模型和 WARP 共用此选择器。

“回看”播放所选范围及前后各一秒，结束后暂停。“填入审核范围”只写入可修改的草稿，加入并保存才形成正式审核。时间轴支持左右键 0.5 秒、Shift 加左右键 5 秒及 Home/End。阶段导航不表示动作已成功完成。

技术分从 100 分制改为 10 分制，例如 92.3 → 9.23。总分、分项、加权贡献、列表、API 和新导出使用相同单位。评分格式版本为 3，接口提供 `max_score: 10`；方法版本仍为 2。A–D 门槛、百分比和 WARP 相对速度单位保持原义。

本轮改进界面和分数表示，不修改神经网络权重、模型判断或人工审核。现有浏览器主题、侧栏和模型选择会从旧品牌键迁移。启动不再预设开发机器的采集目录。

当前源码发布候选提供合成 MCAP 生成器及独立测试，说明见 [README](README.zh-CN.md)。基础查看器来源授权待核实，公开发布状态见 [LICENSE_STATUS.md](LICENSE_STATUS.md)。
