> 历史开发记录。当前 GitHub 安装步骤见 [模型说明](docs/MODELS.md)，复算输入及代码入口见 [训练说明](docs/TRAINING.md)。历史交接包中的私有数据与实验输出不随 GitHub 源码发布。

# G1 运动感知阶段模型 v3

在录像的“进展”面板选择 **G1 香蕉搬运 · 运动感知模型 v3（研究版）**，模型 ID 为 `g1-stage-v3`。现有 18 段录像已完成评分。v2、v1 和公开 DINOv2 版 WARP 模型仍可在同一列表中使用。

v3 针对“左桌抓香蕉模型，搬到右桌，放入篮子”识别五个阶段。它在 v2 的双相机、身体与双手状态、时序 Transformer 和阶段注意力基础上，增加相邻采样的视觉变化和关节位置变化输入，并用随机游走软标签训练阶段过渡区域。DINOv2 图像编码器保持冻结，重新训练的是三成员阶段网络，每个成员 353,610 个参数。

现有录像五折开发验证的宏 F1 从 93.68% 提高至 95.14%，139 个有效标注点中多判对 2 个。历史 5 段录像仍是 65/70，没有进一步提高。开发数据已反复使用，标签为助手看视频生成的研究标注，尚未通过独立人工验收；这些结果不能代表新场景准确率或最终入篮成功率。完整实验和未采用方案见 [升级验证报告](research/stage_v3_motion_20260917/升级验证报告.md)。

界面保留低把握度、模型分歧和输入不完整的复核提示。新增“阶段证据不足”提示，防止概率校准消除模型原有的不确定信号；它不改动阶段预测。历史录像仍有 2 个阶段错误没有触发复核。“释放后观察”仍须结合视频判断物体是否真正入篮。

## 输入和输出约定

头部左目与右腕图像按 2 Hz 提取冻结 DINOv2 特征；输入最近 8 个采样点，跨度 3.5 秒。关节输入为 29 个身体角度和左右手各 6 个实测位置，按最近的过去值对齐，超过 100 ms 视为缺失。新增的位置差分并非传感器实测速度。

每个差分只有在前后采样均有效时参与计算。两个相机都缺失时不输出阶段；单视角或状态缺失时输出降级估计并提示复核。推理只使用当前和过去的输入，不强制动作按固定顺序前进。

阶段、阶段进度、概率和复核原因随模型及数据指纹一起缓存。阶段进度为训练标签导出的辅助量，不能当作任务成功概率或 WARP 动作块权重导出。程序不自动保存人工审核结论。

## 复算

从项目根目录（交接包解压后的根目录）运行。依赖见研究目录 `environment.json`；交接包不附带虚拟环境。

```bash
robot-data-studio/workspace/warp-env/bin/python research/stage_v3_motion_20260917/verify_delivery.py
```

该脚本强制读取包内相对路径下的视觉特征、状态特征和模型，逐一核对 18 段评分，不需要原始 MCAP、登录或 DINOv3 权限。重新解码视频和提取特征则需要原始 MCAP。

两个训练实验目录均已冻结。重训时创建新的兄弟目录，复制目标实验的 `train.py`、`protocol.json` 和 `freeze.json`，建立空 `training/`，保留共享的 `stage_v3_20260917/train.py`、v2 五折参考权重以及 v1/v2 标签与特征目录。运行新目录的 `train.py`。运动实验训练两个候选，各进行 5 折 × 3 种子拟合；根据开发集选择后再做 3 次最终拟合。原目录拒绝覆盖已完成选型。

发布模型的 `training/release.pt` 在 `training/selected.pt` 基础上附加复核规则元数据，全部学习参数和温度不变。`review_guard/` 单独记录这次选型后的复核实验；`finalization.json` 记录发布权重与原分类器的 SHA256 血缘。注册与发布脚本用于记录本轮首次安装，不能直接再次覆盖同一模型 ID。

在 `robot-data-studio` 目录运行工程测试：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=. workspace/warp-env/bin/python -m pytest tests -q
node --test tests/*.test.mjs
```

主要入口是 `warp_progress/stages_v3.py`、`stage_worker_v2.py` 和 `progress_service.py`。新增复核提示在 `static/stage-data.mjs` 与 `static/progress-view.js` 中展示。
