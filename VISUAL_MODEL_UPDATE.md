> 历史开发记录。当前 GitHub 安装步骤见 [模型说明](docs/MODELS.md)，复算输入及代码入口见 [训练说明](docs/TRAINING.md)。历史交接包中的私有数据与实验输出不随 GitHub 源码发布。

# 完整模型与可视化更新 · 2026-09-16

本轮针对用户提供的 PixCLAY / Munari 录屏，修正此前实体、残影和密集轨迹混在一起的问题。

## 当前行为

- 默认使用原始 G1 URDF 和全部 35 份 STL，通过 WebGL 2 显示；原始网格没有进行简化。
- 更新了实体材质、照明、抗锯齿与地面参考阴影。当前实体保持不透明，历史/后续姿态使用蒙版辅助对照，避免把当前机体染花。
- 默认显示实测轨迹，时间窗口为过去 2 秒；目标对照和姿态残影按需开启。显示点做稀疏处理，轨迹线仍来自原始采样。
- 增加全身、上肢近景和下肢近景取景。默认进入 3D 布局，问题与任务进展时间轴可展开，联看/相机布局保留。
- WebGL 失败时明确显示状态，不再自动切到简化实体；用户可选择临时兼容预览。
- 保留本次工作开始前已有的 WARP 进展面板、候选片段和导出字段，没有改变其评分逻辑。

## 验证

桌面验证使用与本机桌面相同的 Wayland 显示环境及独立 Firefox 测试窗口。此前自动化 X11/无头路径失败，不代表实际桌面不能使用完整模型。

- 完整模型桌面运行：30 秒、三路相机和六个前后录制姿态同时开启，约 47 次三维绘制/秒，三路相机约 29 帧/秒；没有未捕获脚本错误。渲染提交耗时 P95 约 5 ms，是 CPU 提交耗时，不是完整 GPU 帧延迟。
- 39 项轨迹/审核界面检查、45 项原有界面检查、35 项工作流检查和 12 项 WARP 进展界面检查通过。
- Python：78 项通过、12 项跳过；Node：33 项通过。可选模型/训练环境相关跳过不表示这些功能已验证。
- 验证中发现本项目虚拟环境继承了系统 SciPy 1.8，与 NumPy 2.2.6 不兼容；已在项目虚拟环境安装已有 requirements-warp.txt 指定的 SciPy 1.15.3，未修改系统包或进展模型算法。
- 原始采集记录与实际审核库未更改。长期运行、所有 GPU/浏览器和所有分辨率仍需继续验证。

证据：`workspace/evidence/visual-polish/desktop-after-result.json`、`desktop-final2-result.json`、`comparison.png` 及各测试日志。

## 数据含义

这是按记录关节状态进行的完整模型回放，没有声称增加了物理仿真。坐标仍为骨盆相对坐标，未补造基座位移；后续姿态来自录制数据，不是预测。G1 实体主要表达 29 个身体关节，双手电机信号仍需结合曲线与视频查看。

此前 2026-09-15 的交接 ZIP 保持原样，它不包含本轮更新。该包中的 HANDOFF_20260915.md 描述的是当时快照。
