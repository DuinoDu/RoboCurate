# 数据质量分级 · v1

> 后续已完成 HFlow / trajlens 规则提取和 500 条公开示范验证，见 [验证报告](QUALITY_VALIDATION_REPORT.md)。本页 A–D 保留为工程基线；新增参考检查不等于这些等级已通过任务质量准确性验证。

本次实现针对上级提出的要求：根据数据本身的特点，从优质到当前不可用划分等级，并展示可核对的依据。

适用范围为现有 G1 / BrainCo 工作流：身体 29 关节、双手各 6 电机的状态与动作、头部和双腕三路 RGB 图像。等级表示已测数值与时序质量；图像内容、接触效果、任务成效和目标训练接口需要另外审核。

## 标准

按最差已测维度决定整段等级，不通过加权平均掩盖关键缺失，也不强制各等级占固定比例。

| 等级 | 判定条件 | 建议处理 |
|---|---|---|
| A · 优质 | 六组状态/动作通道与三路相机齐全；维度正确、数值有限、时间戳有序；30 Hz 对齐覆盖率 ≥ 99%；时长、频率、间隔、跟踪误差和温度未触发当前规则 | 优先完成图像、任务与训练用途审核 |
| B · 良好 | 没有 C / D 问题；覆盖率 ≥ 95%；存在少量覆盖损失，或采样、跟踪误差、温度提醒 | 复核提醒位置，必要时裁剪或补录 |
| C · 需修整 | 覆盖率大于 0 且低于 95%，或有局部 NaN / Inf、时间戳格式/顺序异常 | 定位并修整坏区间，审核保留片段 |
| D · 当前不可用 | 原文件无法读取；必需流缺失、为空、维度错误或整条关键流没有完整有限样本；或共同有效采样数为 0 | 修正数据或补录；当前 G1 训练包导出被阻止 |
| 待评估 | 尚未深度解析，且索引初检未发现明确的关键缺失 | 先完成深度质检，不能将其理解为低质量 |

99% / 95% 是本项目 v1 的初始工程门槛，尚未用训练收益或专家标注数据集校准。频率、间隔、身体跟踪误差与温度采用“分级与质检规则”页面现有阈值；持续偏差和双手误差阈值用于问题导航，不是全部都参与整段等级。

## 指标与证据

- 以整段 MCAP 的起止时间作为范围，30 Hz 建立采样网格。状态、动作和记录中的全部相机在同一时刻满足有效条件才计入分子。
- 状态不跨超过 100 ms 的间隔插值、不外推；动作与图像只选当前或过去的样本，并限制过期时间。覆盖率复用训练预检/导出的 `plan_clip`，使用原始 int64 时间戳。
- 保留有效样本数、总样本数、逐通道无效数量、最长连续有效区间，以及最多五个最长有效区间。有效区间不跨坏点拼接。时间戳异常时不报告失真的覆盖率。
- 身体跟踪误差提醒延用现有 100 Hz 回放信号的整体 P99；它不是控制器时延补偿后的误差或任务失败概率。接触动作和运动速度均可能造成偏差，因此该提醒只将数据列入 B 并要求复核。
- 每个分级维度保留名称、等级、实测依据和下一步建议；导出的质量信息包含 `grading.version`、门槛快照、指标与判定依据。
- 覆盖率只证明数值与图像时间索引覆盖，不证明图像清晰、无遮挡或 JPEG 全部可解码。JPEG 完整性仍在训练导出时逐张检查。

## 与人工审核的关系

自动数据等级与人工使用等级独立。任务指令不完整、采集端成败标记、原 `training_eligible=false`、转换流程限制均保留为上下文/审核待办，不降低自动数据等级，也不会被自动分级解除。

人工 A / B 才能申请训练导出，仍需具体任务指令、深度解析、来源一致与片段预检。自动 D 无法通过手动设为 A 绕过；原始文件保留。自动 C 可以在人工裁剪/修整后审核保留片段，实际导出的有效掩码继续生效。

字段 `quality.suggested_grade` 现在对应自动数据等级，待评估为空字符串；新增详情位于 `quality.grading`。当前 `quality.score` 与 `quality.rating.score` 相同，采用 0–10 分；A–D 分级仍独立使用门槛。人工 `review.grade`、审核数据库与导出格式标识保持兼容。

## 当前数据结果与验证

- 18 段正式采集：均为 B，数值/时序覆盖约 99.67%–99.92%，身体跟踪误差超过当前 0.5 rad 提醒线。其中两段还触发温度提醒。未为了区分档位额外惩罚采集端标记失败的记录，其任务成效仍需人工判定。
- 独立 `mcap_0` 参考样例：D，原因是缺少双腕相机，不满足当前三相机工作流；这不代表其头部图像本身损坏。
- 40 项 Python 测试与 14 项 JavaScript 测试通过；覆盖等级边界、未知不等于差、任务标注不影响数据等级、坏点修整、完整无效、零覆盖、维度错误、时间戳异常和 D 级导出阻断。
- 隔离数据库的 16 项浏览器检查通过：等级分布、筛选、正反排序、人工等级独立、五维解释、覆盖率、待办保留、规则页及窄屏布局；真实审核记录未改变。

本轮未修改回放实现。以上为规则分级与交互验收，不是视觉模型审核或训练效果验收。


## 连续技术评分（方法 v2；评分格式 v3，2026-09-17）

技术评分与 A–D 门槛同时显示。分数用于同规格记录的技术比较，不是任务成功率或模型训练收益。总分与四项分数均为 0–10 分。原 100 分制按十分之一换算，权重和排序不变：

| 分项 | 权重 | 测量依据 |
|---|---:|---|
| 同步覆盖 | 40% | 30 Hz 导出对齐约定下，三路图像及 41 维状态、动作共同有效的时间比例，不含 JPEG 解码 |
| 数值完整 | 15% | 六组状态 / 动作通道的有限数值行比例，各组等权 |
| 采样连续 | 15% | 身体状态、动作、三路相机中最弱一路的频率达标率、正常间隔覆盖时间与正向间隔比例的最小值 |
| 跟踪一致 | 30% | 双腿、腰、双臂、双手共七部位，在各自提醒线以内的时刻占比，部位等权；不补偿控制延迟 |

界面可展开每项查看权重、贡献和扣分量。修改现有跟踪、手部、频率或间隔提醒线会重新计算有关分项。缺维度或时间顺序异常时不合成分数；D 显示关键阻断原因。高分不能平均掉 A–D 的关键门槛，也不能替代导出前检查。

权重为本工作流的初始工程设置，未标定到训练收益。当前 18 段的技术分为 9.23–9.42，说明数值/时间较完整而跟踪存在差异；不人为拉伸分数以制造好坏差距。缺少具体任务、图像内容或采集结果标记不作为技术分的固定扣分项。旧版按问题数量扣分已移除。
